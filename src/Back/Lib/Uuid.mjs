/**
 * Backend wrapper for UUID functions been used in './Shared/' area of code.
 * See 'teqfw.json' descriptor, '@teqfw/di/replace' section.
 *
 * @namespace TeqFw_Core_Back_Lib_Uuid
 */
import {v4, validate} from 'uuid';

export {
    v4,
    validate
}
